# Group: The MySQLConnectionPool with a real database
#   Feature: successfully configure and communicate with the local database
#######################################################################################################################

db {
  alpha {
    driver = "mysql-async"
    url = "mysql://alphauser@localhost/play_driver_test_alpha"
    password = "testpass"
    default = true
  }

  beta {
    driver = "mysql-async"
    url = "mysql://betauser@localhost/play_driver_test_beta"
    password = "testpass"
  }
}
