# Group: MySQLConnectionPool Database Configuration Builder
#   Feature: throw a ConfigurationException on a bad URI
#######################################################################################################################

db.default {
  asyncDriver = "mysql-async"
  url = "|4mysql\\user:password@localhost:432/dbname"
}
