# Group: PostgreSQLConnectionPool Database Configuration Builder
#   Feature: throw a ConfigurationException on a bad URI
#######################################################################################################################

db.default {
  driver = "postgresql-async"
  url = "|4postgresql\\user:password@localhost:432/dbname"
}
