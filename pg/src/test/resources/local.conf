# Group: The PostgreSQLConnectionPool with a real database
#   Feature: successfully configure and communicate with the local database
#######################################################################################################################

db {
  alpha {
    asyncDriver = "postgresql-async"
    url = "postgresql://alphauser@localhost/play_driver_test_alpha"
    password = "testpass"
    default = true
  }

  beta {
    asyncDriver = "postgresql-async"
    url = "postgresql://betauser@localhost/play_driver_test_beta"
    password = "testpass"
  }
}
